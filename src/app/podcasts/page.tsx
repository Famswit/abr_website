import PodcastDetails from '@/components/podcastPage/PodcastDetails'
import PodcastEpisodes from '@/components/podcastPage/PodcastEpisodes'
import React from 'react'

const page = () => {
  return (
    <div>
      <PodcastDetails />
      <PodcastEpisodes />
    </div>
  )
}

export default page
